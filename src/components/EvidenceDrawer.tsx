export function EvidenceDrawer() {
  return (
    <details className="disclosure"><summary>What is real here?</summary><p>The obligation, destination, delivery attempt, and audit events are persisted in PostgreSQL. All names and financial information are synthetic.</p></details>
  );
}
